SELECT  first_name, salary
FROM employees
WHERE salary = 9000;

SELECT  first_name, salary
FROM employees
WHERE salary > 9000;

SELECT  first_name, salary
FROM employees
WHERE salary < 9000;

SELECT  first_name, salary
FROM employees
WHERE salary >= 9000;

SELECT  first_name, salary
FROM employees
WHERE salary <= 9000;

SELECT  first_name, salary
FROM employees
WHERE salary BETWEEN 9000 AND 15000;